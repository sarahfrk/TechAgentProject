package Traitement;
//ici
import Interface.MainFrame;

public class Main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		MainFrame window = new MainFrame();
	//	window.setVisible(true);
		
		
		/*	Object[] argv=new Object[1]; 	
	 try {
			Runtime rt = Runtime.instance();
	     	ProfileImpl p= new ProfileImpl("localhost", 1099, "RSHP");
		   	ContainerController mc = rt.createMainContainer(p);
		   	AgentController ag1 = mc.createNewAgent("Vendeur0", Vendeur.class.getName() ,null);
	        AgentController ag2 = mc.createNewAgent("Vendeur1",  Vendeur.class.getName() ,null);
	        AgentController ag3 = mc.createNewAgent("Vendeur2",  Vendeur.class.getName() ,null);
			argv[0]="p1"; 
		    AgentController agv = mc.createNewAgent("Acheteur",  Acheteur.class.getName() ,argv);
	        
		    agv.start(); 
		    ag1.start(); 
		    ag2.start();
		    ag3.start();
	        	
		} catch (Exception e) { 	e.printStackTrace();   }
		*/
	}



}

